# Die erste Zeile ausgeben
Auf Ihrem Weg zu diesem nächsten Schatz haben Sie schon einiges über die Grundlagen der Python-Programmiersprache gelernt.

Sie erinnern sich bestimmt auch daran, welche Zeile wir bei einem neuen Programm meistens als erstes in der Kommandozeile ausgeben lassen: `Hello, world!`.

Wie machen wir das in Python? Wählen Sie die richtige Antwort aus!

(**TODO:** SingleChoiceAnswer)
- `print Hello, world!` -> falsch
- `print("Hello, world!")` -> richtig
- `print('Hello, world!')` -> richtig
- `"Hello, world!".print()` -> falsch
- `print -> "Hello, world!"` -> falsch
