# Das Spielfenster erzeugen
TODO:
- "Kommentare" lesen
- `import pygame` statt `from pygame import *`
- Wollen in einem ersten Schritt ein leeres Spielfenster erzeugen
- Grösse und Titel festlegen
- Endlosschleifen mit Abbruchbedingung
- display.flip()
- speichern als breakout.py (so, dass man es wieder findet)

```python showLineNumbers
import pygame

# Spiel initialisieren
pygame.init()

# Einstellungen für das Fenster
window_width = 800
window_height = 600

# Hintergrundfarbe festlegen
background_color = pygame.color.Color('black')

# Pygame-Fenster vorbereiten
pygame.display.set_caption("Breakout 👾")
screen = pygame.display.set_mode([window_width, window_height])

run = True
while run:
    screen.fill(background_color)

    pygame.display.flip()

    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            run = False
```

---
