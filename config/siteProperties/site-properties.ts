import {gbslSiteProperties} from "./gbsl.site-properties";
import {lerbermattSiteProperties} from "./lerbermatt.site-properties";
import {teachSiteProperties} from "./teach.site-properties";
import {draftsSiteProperties} from "./drafts.site-properties";

export const siteProperties = {
  gbsl: gbslSiteProperties,
  lerbermatt: lerbermattSiteProperties,
  teach: teachSiteProperties,
  drafts: draftsSiteProperties,
};
