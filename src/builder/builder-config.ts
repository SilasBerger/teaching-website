/**
 * Any source segment matching this pattern will be ignored when recursing subdirectories specified by a mapping. It
 * has no effect at the top level of a mapping.
 */
export const SOURCE_SEGMENT_IGNORE_PATTERN = /.*\.version-\.*/;
